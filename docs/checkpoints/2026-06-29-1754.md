# Checkpoint — 2026-06-29 17:54

## Session summary

Короткая постсессия после возобновления `2026-06-29-1059`. Пользователь
вернулся с большой партией локальных правок и попросил сделать один
коммит «как есть» + завести таску на покрытие тестами, ничего больше не
трогать.

Сделано в этом окне:

- **Коммит `b2a96ce` — feat: autodump on exit + captrack-pgo
  wire/unwire subcommands** (8 файлов, +732 строки). Содержит два
  независимых куска pipeline plumbing:
  - `src/autodump.rs` — под фичей `telemetry` регистрирует
    `#[ctor::dtor]` хук, который пишет
    `target/captrack-pgo/profile-<binary_stem>.json` после `main`.
    Опт-аут `CAPTRACK_AUTODUMP=0`, директория `CAPTRACK_DUMP_DIR`.
  - `captrack-pgo wire`/`unwire` — патчит каждый Cargo.toml целевого
    workspace, добавляя `captrack` как зависимость (root → в
    `[workspace.dependencies] features=["telemetry"]`, члены → в
    `[dependencies] { workspace = true }`). Снапшот в
    `target/captrack-pgo/last-wire.json` для lossless revert.
    Идемпотентно — уже-wired Cargo.toml пропускаются.
- **Таска #45** заведена с подробным чек-листом на оба модуля
  (`autodump` секция: default_dump_path / autodump_enabled / e2e binary
  smoke; `wire/unwire` секция: resolve_workspace_members, patch_root,
  patch_member, run_wire/unwire круги, sha verification, version
  guard).
- **Изучение 11 новых коммитов** (`4dbb471..256f9ca`, локально, не
  запушены). Это финал Path C/D на реальном workspace shamir-db.
  Группы:
  1. `4dbb471` — atomic write `<tmp>+rename` + periodic
     `#[ctor::ctor]` поток с `CAPTRACK_AUTODUMP_INTERVAL_MS` (дефолт
     500мс, было 2000мс), SIGKILL-resilient.
  2. End-to-end фиксы на shamir-db (`e1a972d`, `786da1c`, `c8057ab`):
     Windows backslash escape (`\f`/`\b` ломали Rust string literal в
     `auto_label`), data-flow guard в `is_safe_instrument_context`
     (HIR Visitor собирает все usages, классифицирует через pure
     `classify_parent_kind`, 9 unit тестов + coverage assert),
     `--allow-dirty` после `--` для cargo dylint 6.0.1,
     `TrackedBytesMut::freeze(self)` через ptr::read + forget.
  3. Расширение покрытия типов: enum `TrackedType` (14 вариантов,
     +path-based fallback через `def_path_str`), `TrackedSmallVec`
     (14-й тип), `wrap_from(inner, name, …)` на всех 14 типах
     (фундамент Phase K) + 22 unit-теста.
  4. `30cbb70` — universal `wrap_from` instrument: lint оборачивает
     оригинальное выражение (`vec![a,b,c]`/`smallvec![x;n]`/любой
     constructor), не подменяет его → закрывает #338 (потеря
     элементов). Распознаёт макросы через `is_expn_of` +
     `span.source_callsite()`. 92 lib + 25 integration тестов, на
     shamir-db 19 файлов / 24 сайта инструментировано.
  5. `afd492a` — `Default::default()` Strategy B: синтезирует
     типизированный путь (`::std::vec::Vec::with_capacity(N)`, +hasher),
     11 типов рекогнайз, BTree* остаются warning-only.
  6. `01691f6` — per-type integration suite (`tests/per_type.rs`, 25
     тестов через `cargo dylint --fix` на синтетических крейтах,
     per-test temp dir, новые dev-deps `assert_cmd`+`tempfile`).
  7. `256f9ca` — `.gitignore` для `rustc-ice-*.txt`.

Импликации для #45: instrument lint и `wrap_from` уже покрыты тестами
косвенно (122 lib + 25 integration). Скоуп таски сужается до
непокрытого: (a) `src/autodump.rs` (default_dump_path, autodump_enabled,
atomic write, periodic thread, CAPTRACK_AUTODUMP_INTERVAL_MS дефолт=500),
(b) `captrack-pgo/src/wire.rs` (resolve_members, patch_root,
patch_member, run_wire/unwire).

11 коммитов от пользователя (от `4dbb471` до `256f9ca`) НЕ запушены —
пользователь не давал команду push. Также не запушен `b2a96ce` (мой
коммит этой сессии). Итого 12 коммитов локально перед origin/main
(который стоит на `dfbe57b`).

Активных таймеров (babysit / cron) нет.

## Active goal

```
/goal реализуй план, используй агентов "sxx (agent)"
```

(Из предыдущей сессии. Stop-hook надо перевзвести вручную если хочется
держать активным — `/checkpoint` сам не трогает hook.)

## TaskList
### in_progress
(нет)

### pending
- #45 Покрыть тестами autodump + wire/unwire

### recently completed
(нет в этом окне — #44 и ниже закрыты предыдущими сессиями)

## Decisions

- **Один коммит на два независимых куска** (autodump + wire/unwire)
  вместо двух — пользователь сформулировал «нам добавили изменения.
  сделай коммит» в единственном числе, оба куска — pipeline plumbing
  одного шага.
- **Checkpoint предыдущей сессии (`2026-06-29-1059.md`) НЕ добавлен в
  коммит** — checkpoint-skill явно требует «не коммитить
  автоматически, оставить пользователю».
- **Скоуп таски #45 сужен** после изучения новых коммитов: instrument
  lint и `wrap_from` уже имеют тесты, не дублировать.
- **`git push` НЕ сделан** — глобальный constraint: «никогда не делать
  push без явной просьбы». 12 коммитов лежат локально.
- **Использование агентов в этой сессии — нет** — пользователь не
  просил, изучение коммитов сделано прямыми Bash вызовами `git show
  --stat`.

## Open questions

- **Push?** 12 коммитов локально (`4dbb471..256f9ca` + `b2a96ce`). В
  конце ответа об изучении коммитов спросил «Запушить или ещё
  подержать локально?» — ответа не получил, ушёл checkpoint.
- **Скоуп таски #45.** Описание уже сужено до autodump + wire/unwire,
  но не зафиксировано явно что atomic write / periodic thread из
  `4dbb471` тоже входят. На усмотрение исполнителя при запуске.
- **Flaky тест `tbtreeset_records_peak_and_count`** — тянется с
  предыдущих сессий, в работе ничего не делалось.

## Repo state

```
?? docs/checkpoints/2026-06-29-1059.md
```

```
256f9ca chore: ignore rustc-ice-*.txt crash dumps from lint test runs
01691f6 test(captrack-pgo-lint): per-type integration tests for all 14 TrackedTypes (Phase I)
afd492a feat(captrack-pgo-lint): Default::default() Strategy B suggestion synthesis (Phase G)
30cbb70 feat(captrack-pgo-lint): universal wrap_from instrument + macro recognition (Phases F + K + #338 fix)
f1a5b8d feat(captrack): wrap_from method on all 14 TrackedX types (Phase K)
```
