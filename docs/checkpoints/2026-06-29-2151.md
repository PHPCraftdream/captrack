# Checkpoint — 2026-06-29 21:51

## Session summary

Третий чекпойнт за день. Постсессия после `2026-06-29-1754` — пользователь
снова попросил «изучи новые коммиты», в этот раз 5 свежих от него
(`7e41da4..872c377`), завершающих M-цикл на реальном workspace shamir-db
и принесших **первый измеримый PGO-win** (tx_pipeline −9.6%, p<0.05).

В этом окне сессии я **не писал кода** — только изучение коммитов через
`git show --stat` и развёрнутый rapport пользователю. Никаких файлов
кроме чекпойнтов не создавал, никаких task-ов не двигал, push не делал.

Что было изучено (5 коммитов, по фазам):

1. **`7e41da4` — Phase M: 3 бага apply-пайплайна**. Три независимых бага,
   каждый сам по себе блокировал end-to-end:
   - формат профайла (raw captrack dump vs ожидаемый `Vec<SiteStats>` —
     теперь через temp file конвертация);
   - SiteKey строился из позиции уже инструментированного `wrap_from(...)`
     — фикс через `parse_name()` извлекает оригинальный (file,line,col)
     из `"auto:..."` labelа;
   - `cargo fix` incremental cache → молчаливое "no suggestions" — фикс
     `--all-targets` после `--`.
   Валидация: 1 реальный патч (`wal_segment.rs:212` Vec::with_capacity(32768)).

2. **`8f63a9d` + `0574972` — Phase L: `CapInspect`**. Главная
   архитектурная вещь цикла. `wrap_from` менял тип на `TrackedX` →
   блочил escape positions (return / struct field / fn arg /
   type-ascribed let), data-flow guard пропускал ~75% сайтов на
   shamir-db (24 wrap_from, 6 fired). Решение — borrow-only инспекция в
   точке потребления: `{ ::captrack::CapInspect::cap_inspect_at(&v, ...); v }`.
   Тип v не меняется → работает в любой позиции. Записывает peak at
   consumption (после возможного роста — строго полезнее, чем capacity
   at construction). 14 impl'ов под telemetry, off-feature no-op. Lint
   эмитит wrap_from для safe usages + cap_inspect для unsafe + оба
   режима коэкзистят. Идемпотентность через source-snippet inspection.
   На shamir-db: **24 → 316** проектированных мест.

3. **`1a10978` — Phase N: multi-span hasher swap для ascribed let'ов**.
   Раньше `let m: HashMap<K,V> = HashMap::new()` пропускался (E0308).
   Теперь dylint `multipart_suggestion` — два span'а атомарно (правка
   ascription + конструктор). `AscriptionForm::{HasherOmitted,
   HasherPinned, Wildcard, NotApplicable}` decision table.
   Lifetime-aware подсчёт generic args. `hasher_arg_counts(t)` для всех
   7 hash-keyed типов.

4. **`872c377` — Phase O: THasher-aware**. В реальных проектах часто
   `type THasher = BuildHasherDefault<FxHasher>` (shamir-db
   `shamir_collections::THasher`). Раньше `HasherPinned` слепо
   пропускал. Теперь `HasherKind::{FastKnown, SlowDefault, Unknown}`
   через pure `classify_hasher_snippet` + `FAST_HASHER_SNIPPETS` const
   array. Соответствующие сообщения, пользователь больше не теряется.

End-to-end на shamir-db (Phase L+N+O combined): 41 файл / 123 сайта
инструментировано, 15 fired, 4 капасити-патча применены, **tx_pipeline
−9.6% p<0.05** — первый measurable PGO-win. Phase O не сработал в
бенчах (peak=0 во всех HashMap-сайтах в этом ране — отдельная
структурная проблема).

Импликации для таски #45: новые коммиты затрагивают lint/трекеры, не
трогают `src/autodump.rs` или `captrack-pgo/src/wire.rs` → скоуп #45
остаётся ровно тот же (autodump knobs + wire/unwire e2e).

**17 коммитов локально не запушены** (`b2a96ce..872c377`),
origin/main стоит на `dfbe57b`. Push на слово пользователя.

Активных таймеров (babysit / cron) нет.

## Active goal

```
/goal реализуй план, используй агентов "sxx (agent)"
```

(Тянется с предыдущих сессий. Не перевзвешивался — `/checkpoint` hook не
трогает.)

## TaskList
### in_progress
(нет)

### pending
- #45 Покрыть тестами autodump + wire/unwire

### recently completed
(нет в этом окне — таска #45 — единственная активная, все ниже
завершены ранее)

## Decisions

- **Не агентов**: пользователь сказал «изучи новые коммиты», запустил
  параллельные `git show --stat` напрямую. Глобальный constraint:
  «никогда не вызывать под-агентов без явной просьбы».
- **Скоуп #45 не расширен**: новые 5 коммитов не покрыли autodump или
  wire/unwire, поэтому таска остаётся ровно в формулировке предыдущего
  чекпойнта.
- **Push не делал**: 17 коммитов локально, ждут явной команды.

## Open questions

- **Push?** 17 коммитов локально (`b2a96ce..872c377`). Вопрос
  откладывается с предыдущего чекпойнта — пользователь не ответил, ушёл
  в новый раунд `/checkpoint`.
- **Phase O peak=0 на shamir-db HashMap-сайтах** — отмечено в коммите
  как «отдельная структурная проблема». Не моё расследование, не моя
  таска — пользовательская заметка.
- **Flaky `tbtreeset_records_peak_and_count`** — тянется с предыдущих
  сессий.

## Repo state

```
?? docs/checkpoints/2026-06-29-1059.md
?? docs/checkpoints/2026-06-29-1754.md
```

```
872c377 feat(captrack-pgo-lint): THasher-aware hasher swap (Phase O)
1a10978 feat(captrack-pgo-lint): multi-span hasher rewrite for ascribed lets (Phase N)
0574972 feat(captrack-pgo-lint): Phase L injection — cap_inspect at usage points
8f63a9d feat(captrack): CapInspect trait for consumption-point telemetry (Phase L)
7e41da4 fix(captrack-pgo): end-to-end apply pipeline (3 bugs found via Phase M)
```
