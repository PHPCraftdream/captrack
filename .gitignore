/target/
**/*.rs.bk
Cargo.lock.bak
.idea/
.vscode/
*.swp
.DS_Store

# rustc Internal Compiler Error crash dumps — emitted under
# .../<crate>/rustc-ice-<timestamp>-<pid>.txt during lint development
# (touches nightly internals on `cargo test --test per_type`).
rustc-ice-*.txt
